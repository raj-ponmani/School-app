# School-app
building a school registration from using django

Requirements needed to run this app in your machine:
  1. django (command : pip install django)
  2. mysqlclient (command : pip install mysqlclient)
  3. MySql database (i used MySql database in this app)
